# Основні цілі проекту включають:
# - Ознайомлення зі структурою та змістом датасету COVID-19,
# вивчення різних аспектів даних, включаючи демографічні
# показники, показники захворюваності та вакцинації.
# - Проведення первинного аналізу даних, для цього
# використаємо статистичні методи та візуалізацій для
# виявлення загальних тенденцій та закономірностей.
# - Проведемо очищення даних, обробка пропущених значень
# та створення нових ознак для підвищення точності моделей.
# - Побудуємо класифікаційні та регресійні моделі для
# прогнозування ключових показників пандемії.
# - Проведемо аналіз отриманих результатів для прийняття
# обґрунтованих рішень щодо подальших досліджень та заходів.


# 1. Захворюваність та смертність
# total_cases - Загальна кількість випадків COVID-19
# new_cases - Нові випадки за день
# new_cases_smoothed - Згладжене середнє за 7 днів (прибирає "шум" вихідних)
# total_deaths - Загальна кількість смертей
# new_deaths - Нові смерті за день
# 2. Показники на населення
# total_cases_per_million - Випадки на мільйон населення
# total_deaths_per_million - Смерті на мільйон населення
# new_cases_per_million - Нові випадки на мільйон за день
# 3. Медична система
# reproduction_rate - Скільки людей в середньому заражає один хворий
# icu_patients - Пацієнти у реанімації
# hosp_patients - Пацієнти в лікарнях
# hospital_beds_per_thousand - Лікарняні ліжка на 1000 населення
# 4. Тестування
# total_tests - Загальна кількість тестів
# positive_rate - Відсоток позитивних тестів
# tests_per_case - Скільки тестів на один виявлений випадок
# 5. Вакцинація
# total_vaccinations - Загальна кількість введених доз
# people_vaccinated - Люди з хоча б однією дозою
# people_fully_vaccinated - Повністю вакциновані
# total_boosters - Бустерні дози
# 6. Ідентифікатори
# iso_code - Код країни (UKR, USA, DEU...)
# continent - Континент
# location - Назва країни
# date - Дата запису

import os
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Створюємо папку для збереження графіків, якщо її немає
os.makedirs("charts", exist_ok=True)

# ==============================================================================
# Завдання 1. Підготовка
# ==============================================================================
# Проведіть аналітику на тему COVID-19 та напишіть короткий текст (150-200 слів)
# про те, чому аналіз даних COVID-19 важливий для:
# - Суспільства
# - Охорони здоров'я
# - Економіки
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 1. ПІДГОТОВКА (Аналітичний огляд)")
print("="*80)
task1_text = """
Аналіз даних COVID-19 відіграє критично важливу роль для трьох основних сфер нашого життя: суспільства, охорони здоров'я та економіки.
Для суспільства об'єктивні дані є головним джерелом достовірної інформації. Вони допомагають громадянам зрозуміти реальний рівень
загрози, оцінити ефективність карантинних заходів та протидіяти дезінформації. Прозорість у статистиці захворюваності формує суспільну
довіру до рішень уряду та мотивує дотримуватися рекомендацій, таких як носіння масок або вакцинація.
Для системи охорони здоров'я аналіз даних — це інструмент стратегічного планування та порятунку життів. Він дозволяє прогнозувати спалахи
інфекції, оцінювати завантаженість лікарень та реанімаційних відділень, а також розподіляти обмежені ресурси: ліжка, апарати ШВЛ, ліки
та вакцини. Також дані допомагають досліджувати мутації вірусу та оцінювати ефективність лікувальних препаратів.
Для економіки аналітичні моделі є основою для балансування між збереженням здоров'я людей та мінімізацією фінансових втрат.
Аналіз динаміки дозволяє вводити точкові, локальні обмеження замість тотальних локдаунів, прогнозувати відновлення ринків,
підтримувати постраждалі галузі та планувати державні бюджети в умовах невизначеності.
Отже, аналітика даних є фундаментом для прийняття обґрунтованих рішень на всіх рівнях.
"""
print(task1_text.strip())
print(f"\nКількість слів у тексті: {len(task1_text.split())}")
print("="*80)


# ==============================================================================
# Завдання 2. Завантаження та первинний огляд
# ==============================================================================
# Завантажте датасет COVID-19 та імпортуйте його.
# Використайте head(), info() та columns.
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 2. ЗАВАНТАЖЕННЯ ТА ПЕРВИННИЙ ОГЛЯД")
print("="*80)
url = "https://catalog.ourworldindata.org/garden/covid/latest/compact/compact.csv"
print(f"Завантаження даних з посилання: {url}...")
df = pd.read_csv(url)

print("\nПерші 5 рядків датасету (head):")
print(df.head())

print("\nСписок стовпців датасету (columns):")
print(df.columns.tolist())

print("\nІнформація про типи даних та структуру (info):")
df.info()
print("="*80)


# ==============================================================================
# Завдання 3. Очищення даних
# ==============================================================================
# Використайте isnull() та sum() для виявлення відсутніх значень.
# Вирішіть, як їх обробити. Перевірте та видаліть дублікати.
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 3. ОЧИЩЕННЯ ДАНИХ")
print("="*80)
print("Кількість пропущених значень у кожному стовпці (топ-10 за спаданням):")
print(df.isnull().sum().sort_values(ascending=False).head(10))

# Рішення щодо обробки пропущених даних:
# 1. Стовпець human_development_index повністю пустий (всі значення пропущені), тому видаляємо його.
if 'human_development_index' in df.columns:
    df = df.drop(columns=['human_development_index'])
    print("\n- Стовпець 'human_development_index' видалено через відсутність даних.")

# 2. Для числових стовпців випадків та смертей пропущені значення заповнюємо нулями,
# оскільки відсутність записів зазвичай свідчить про нульову кількість випадків.
numeric_fill_cols = [
    'total_cases', 'new_cases', 'new_cases_smoothed',
    'total_cases_per_million', 'new_cases_per_million', 'new_cases_smoothed_per_million',
    'total_deaths', 'new_deaths', 'new_deaths_smoothed',
    'total_deaths_per_million', 'new_deaths_per_million', 'new_deaths_smoothed_per_million'
]
df[numeric_fill_cols] = df[numeric_fill_cols].fillna(0)
print(f"- Пропущені значення у ключових числових стовпцях захворюваності та смертності заповнено 0.")

# Перевірка на дублікати
duplicates_count = df.duplicated().sum()
print(f"- Знайдено дублікатів: {duplicates_count}")
if duplicates_count > 0:
    df = df.drop_duplicates()
    print("- Дублікати успішно видалені.")
print("="*80)


# ==============================================================================
# Завдання 4. Статистичний огляд
# ==============================================================================
# Визначте розподіл, середні значення, медіану тощо.
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 4. СТАТИСТИЧНИЙ ОГЛЯД")
print("="*80)
print("Загальна статистика для ключових показників по всьому світу:")
print(df[['total_cases', 'new_cases', 'total_deaths', 'new_deaths']].describe())

# Окремий статистичний огляд для України
print("\nСтатистика ключових показників для України (Ukraine):")
ukr_df = df[df['country'] == 'Ukraine'].copy()
print(ukr_df[['total_cases', 'new_cases', 'total_deaths', 'new_deaths']].describe())

print("\nМедіанні значення для України:")
print(ukr_df[['total_cases', 'new_cases', 'total_deaths', 'new_deaths']].median())
print("="*80)


# ==============================================================================
# Завдання 5. Візуалізація
# ==============================================================================
# Створення лінійних графіків динаміки випадків і смертей для України.
# Створення бар-чарту порівняння кількості випадків між континентами на останню дату.
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 5. ВІЗУАЛІЗАЦІЯ")
print("="*80)

# Налаштовуємо стиль графіків
sns.set_theme(style="whitegrid")

# 1. Лінійний графік для України
ukr_df['date'] = pd.to_datetime(ukr_df['date'])
ukr_df = ukr_df.sort_values(by='date')

fig, ax1 = plt.subplots(figsize=(12, 6))

color = 'tab:blue'
ax1.set_xlabel('Дата', fontsize=12, labelpad=10)
ax1.set_ylabel('Загальна кількість випадків (total_cases)', color=color, fontsize=12)
ax1.plot(ukr_df['date'], ukr_df['total_cases'], color=color, linewidth=2, label='Випадки')
ax1.tick_params(axis='y', labelcolor=color)
ax1.yaxis.set_major_formatter(plt.FuncFormatter(lambda x, p: f'{x:,.0f}'))

# Додаємо другу вісь Y для смертей
ax2 = ax1.twinx()
color = 'tab:red'
ax2.set_ylabel('Загальна кількість смертей (total_deaths)', color=color, fontsize=12)
ax2.plot(ukr_df['date'], ukr_df['total_deaths'], color=color, linewidth=2, linestyle='--', label='Смерті')
ax2.tick_params(axis='y', labelcolor=color)
ax2.yaxis.set_major_formatter(plt.FuncFormatter(lambda x, p: f'{x:,.0f}'))

plt.title('Динаміка випадків захворювання та смертей від COVID-19 в Україні', fontsize=14, fontweight='bold', pad=15)
fig.tight_layout()
plot_path_ukr = "charts/ukraine_trends.png"
plt.savefig(plot_path_ukr, dpi=300)
plt.close()
print(f"- Лінійний графік динаміки захворюваності та смертей для України збережено в '{plot_path_ukr}'.")

# 2. Стовпчиковий графік за континентами на останню дату
latest_date = df['date'].max()
countries_latest = df[(df['date'] == latest_date) & (df['continent'].notna())]
continent_data = countries_latest.groupby('continent')['total_cases'].sum().reset_index()
continent_data = continent_data.sort_values(by='total_cases', ascending=False)

plt.figure(figsize=(10, 6))
barplot = sns.barplot(
    data=continent_data,
    x='continent',
    y='total_cases',
    hue='continent',
    legend=False,
    palette='viridis'
)

# Форматуємо вісь Y в мільйони
plt.gca().yaxis.set_major_formatter(plt.FuncFormatter(lambda x, p: f'{x/1e6:.1f} млн'))
plt.xlabel('Континент', fontsize=12)
plt.ylabel('Загальна кількість випадків (млн)', fontsize=12)
plt.title(f'Порівняння загальної кількості випадків COVID-19 за континентами на {latest_date}', fontsize=14, fontweight='bold', pad=15)

# Додаємо підписи значень над стовпчиками
for p in barplot.patches:
    val = p.get_height()
    if val > 0:
        barplot.annotate(f'{val/1e6:.1f}M', 
                         (p.get_x() + p.get_width() / 2., val), 
                         ha='center', va='center', 
                         xytext=(0, 8), 
                         textcoords='offset points', 
                         fontsize=10, fontweight='bold')

plt.tight_layout()
plot_path_cont = "charts/continent_comparison.png"
plt.savefig(plot_path_cont, dpi=300)
plt.close()
print(f"- Стовпчиковий графік за континентами збережено в '{plot_path_cont}'.")
print("="*80)


# ==============================================================================
# Завдання 6. Аналіз
# ==============================================================================
# Напишіть аналіз (200-300 слів) та опишіть аномалії або несподівані результати.
# ==============================================================================

print("\n" + "="*80)
print(" ЗАВДАННЯ 6. АНАЛІЗ ТА АНОМАЛІЇ")
print("="*80)
task6_text = """
1) Аналіз отриманих графіків та статистики:
На основі візуалізації динаміки COVID-19 в Україні видно класичну S-подібну криву зростання (сигмоїду)
для загальної кількості випадків та смертей. До осені 2020 року спостерігалося повільне поширення вірусу,
після чого почалося стрімке експоненціальне зростання. Пікові періоди з найбільшою кількістю нових випадків
та смертей в Україні припали на кінець 2020 року (перша велика хвиля), весну 2021 року (хвиля дельта-штаму)
та кінець 2021 – початок 2022 років (хвиля штаму омікрон), коли щодня реєструвалися десятки тисяч нових
інфікованих та сотні смертей. З весни 2022 року зростання графіків суттєво уповільнилося, стабілізувавшись
на рівні близько 5.55 млн випадків та 109,937 смертей.
У глобальному вимірі лідером за кількістю зареєстрованих випадків є Азія (понад 300 млн випадків) та Європа
(близько 250 млн випадків). Це зумовлено високою густотою населення (в Азії) та надзвичайно розвиненою та
прозорою системою масового тестування (в Європі). Далі за ними йдуть Північна Америка, Південна Америка, Африка та Океанія.
2) Аномалії та несподівані результати:
По-перше, наприкінці 2022 року та впродовж 2023 року спостерігається різке плато на лінійному графіку
України та багатьох інших країн. Це пояснюється не фактичним зникненням вірусу, а припиненням масового
тестування, зміною протоколів обліку та перемиканням уваги суспільства на інші події (зокрема військовий стан в
Україні). По-друге, рівень смертності в Африці виглядає аномально низьким порівняно з чисельністю її населення.
Це може свідчити про слабку систему діагностики та недооблік випадків смерті у багатьох африканських країнах,
а також про молодшу вікову структуру населення континенту. Крім того, на початку пандемії у багатьох країнах
спостерігалися різкі коливання та «сходинки» на графіках, що вказує на затримки у передачі даних лабораторіями
або зміни у методології підрахунку (наприклад, додавання ймовірних випадків без лабораторного підтвердження).
"""
print(task6_text.strip())
print(f"\nКількість слів у аналізі: {len(task6_text.split())}")
print("="*80)
print("\nАналіз повністю виконано успішно.")
