def linear_search(arr, target):
    for index, element in enumerate(arr):
        if element == target:
            return index
    return -1


# ЗАВДАННЯ 1
# Нам потрібно доповнити базову реалізацію лінійного
# пошуку лічильником, який буде підраховувати кількість
# виконаних порівнянь. Це дозволить нам аналізувати,
# як змінюється кількість порівнянь в залежності від вхідних даних.


# ЗАВДАННЯ 2
# Для впорядкованого списку потрібно створити оптимізовану
# версію лінійного пошуку. Якщо поточний елемент більше
# шуканого значення, пошук повинен завершуватися,
# оскільки далі шукати немає сенсу.
# def linear_search_optimized(arr, target):


# ЗАВДАННЯ 3
# Ми маємо створити функцію лінійного пошуку, яка
# знаходить індекс максимального елемента в масиві.
# Також ми порівняємо її результати з використанням
# вбудованих функцій Python max() та index().


# ЗАВДАННЯ 4
# Нам потрібно реалізувати функцію, яка перебирає
# список словників і знаходить перший словник, де
# певний ключ має задане значення. Ми також проаналізуємо,
# як змінюється час виконання пошуку з різними розмірами списку.
# def linear_search_dict(lst, key, value):


# ЗАВДАННЯ 5
# Реалізуйте функцію лінійного пошуку, яка
# повертає всі індекси, де зустрічається шуканий
# елемент, оскільки він може зустрічатися більше одного разу.


# ЗАВДАННЯ 6
# Реалізуйте лінійний пошук у двовимірному масиві
# (список списків), щоб знайти координати (рядок, стовпець)
# першої появи заданого значення.
# ВИМОГИ:
# Створіть функцію linear_search_2d(matrix, target).
# Переберіть рядки та стовпці двовимірного масиву.
# Якщо знайдете елемент, що дорівнює target, поверніть кортеж (row_index, col_index).
# Якщо елемент не знайдено, поверніть (-1, -1).
# def linear_search_2d(matrix, target):
# matrix = [
#     [5, 3, 7],
#     [1, 9, 2],
#     [8, 4, 6]
# ]


# ЗАВДАННЯ 7
# Реалізуйте рекурсивну функцію лінійного пошуку,
# яка знаходить перший індекс появи заданого елемента.
# ВИМОГИ:
# Створіть функцію recursive_linear_search(arr, target, index=0).
# Якщо поточний індекс перевищує довжину списку, поверніть -1.
# Якщо елемент за поточним індексом дорівнює target, поверніть цей індекс.
# Інакше рекурсивно викликайте функцію для наступного індексу.
# def recursive_linear_search(arr, target, index=0):


# ЗАВДАННЯ 8
# Реалізуйте функцію лінійного пошуку,
# яка шукає перший елемент, що перевищує заданий поріг.
# ВИМОГИ:
# Створіть функцію find_first_above(arr, threshold).
# Переберіть список і знайдіть перший елемент, який більший за threshold.
# Поверніть індекс цього елемента або -1, якщо такого елемента немає.
# def find_first_above(arr, threshold):


# ЗАВДАННЯ 9
# Реалізуйте функцію лінійного пошуку з можливістю
# передавання користувацького критерію пошуку через
# лямбда-функцію. Функція має знаходити перший
# елемент, для якого критерію повертає True.
# ВИМОГИ:
# Створіть функцію linear_search_custom(arr, condition), де condition – це лямбда-функція.
# Переберіть список і застосуйте condition до кожного елемента.
# Якщо для елемента condition(element) повертає True, поверніть його індекс.
# Якщо жоден елемент не задовольняє умову, поверніть 1.
# def linear_search_custom(arr, condition):
# result = linear_search_custom(data, lambda x: x % 2 == 0)