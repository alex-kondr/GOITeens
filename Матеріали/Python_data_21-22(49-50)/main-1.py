# def selection_sort(arr):
#     n = len(arr)
#     for i in range(n-1):
#         min_index = i
#         for j in range(i+1, n):
#             if arr[j] < arr[min_index]:
#                 min_index = j

#         if min_index != i:
#             arr[i], arr[min_index] = arr[min_index], arr[i]

#     # return arr

# arr = [8, 1, 5, 4, 2]
# # print(selection_sort(arr))
# selection_sort(arr)
# print(arr)

# ------------------------------------------------------------------
# ЗАВДАННЯ 1
# Реалізуйте алгоритм вибіркового сортування для списку цілих чисел у зростальному порядку.

# Створіть функцію selection_sort(arr), яка приймає список arr.
# Виконайте n−1 ітерацій, де n — довжина arr.
# На i-й ітерації знайдіть індекс найменшого елемента серед arr[i..n-1].
# Обміняйте його з елементом на позиції i.
# Після завершення виведіть або поверніть відсортований список.
# def selection_sort(arr):
#     pass

# numbers = [5, 2, 9, 1, 7]
# selection_sort(numbers)
# print(numbers)
# ------------------------------------------------------------------

# ЗАВДАННЯ 2
# Виводьте стан списку після кожної ітерації “вибору” мінімуму.
# Це допоможе відслідкувати, як мінімальні елементи поступово переміщуються на початок списку.
# Додайте усередині циклу print(f"Після ітерації {i}: {arr}").
# Переконайтеся, що можна побачити, як список змінюється крок за кроком.

# def selection_sort_steps(arr):
#     pass

# nums = [4, 8, 2, 9, 3, 1]
# selection_sort_steps(nums)
# ------------------------------------------------------------------

# ЗАВДАННЯ 3
# Змініть алгоритм так, щоб список сортувався у спадному порядку (від більшого до меншого).
# При пошуку «найменшого» замініть логіку на «пошук найбільшого».
# Обмінюйте найбільший знайдений елемент із позицією i.
# Перевірте роботу на різних прикладах.

# def selection_sort_desc(arr):
#     pass

# test_list = [3, 8, 2, 7, 1]
# selection_sort_desc(test_list)
# print(test_list)
# ------------------------------------------------------------------

# ЗАВДАННЯ 4
# Порахуйте загальну кількість порівнянь, виконаних під час вибіркового сортування.
# Введіть змінну count_comp = 0.
# У внутрішньому циклі, кожного разу при
# if arr[j] < arr[min_index]: ...
# або при arr[j] > arr[min_index]: ...
# (залежно від порядку), вважайте це «одним порівнянням».
# Після завершення виведіть кількість порівнянь.

# def selection_sort_with_comparisons(arr):
#     pass

# data = [4, 3, 2, 1]
# selection_sort_with_comparisons(data)
# print(data)
# ------------------------------------------------------------------

# ЗАВДАННЯ 5
# Додайте “перевірку”, скільки було реальних обмінів (swap) під час усього сортування.
# Введіть змінну count_swaps = 0.
# Якщо min_index != i, робіть обмін і count_swaps += 1.
# Після завершення виведіть цю кількість. Перевірте, що вона має бути щонайбільше n−1.

# def selection_sort_with_swaps(arr):
#     pass

# numbers = [2, 2, 2, 2]
# selection_sort_with_swaps(numbers)
# ------------------------------------------------------------------

# ЗАВДАННЯ 6
# Обробіть список, що містить кортежі або словники, і посортуйте їх за певним полем.
# Припустімо, що є список players = [("Alice", 50), ("Bob", 20), ("Charlie", 50), ("David", 10)].
# Потрібно відсортувати за другим значенням (числовим полем), наприклад, “очки” (scores).
# Знайдіть найменший “score” у підмасиві, обміняйте з i-м.
# Перевірте, що все правильно відсортовано за очками.

# def selection_sort_by_score(players):
#     pass

# players_data = [("Alice", 50), ("Bob", 20), ("Charlie", 50), ("David", 10)]
# selection_sort_by_score(players_data)
# print(players_data)
# ------------------------------------------------------------------

# ЗАВДАННЯ 7
# Виведення “індексу мінімуму” під час кожної ітерації. Це допоможе краще бачити, як алгоритм “вгадує”, який мінімум.
# На i-й ітерації після пошуку виведіть print(f"Min element at index {min_index}, value={arr[min_index]}").
# Тоді обміняйте зі значенням на позиції i.
# Простежте, як змінюються min_index і частина списку, яку вище відсортували.

# def selection_sort_verbose_min(arr):
#     pass

# test_data = [9, 7, 8, 3, 1, 6]
# selection_sort_verbose_min(test_data)
# ------------------------------------------------------------------

# ЗАВДАННЯ 8
# Реалізуйте “укорочення” проходів: після i-го кроку,
# мінімум поставлено на позицію i. Не змінюємо логіку,
# але переконайтеся, що внутрішній цикл пошуку
# мінімуму проходить лише по ділянці [i..n-1].
# Це базова реалізація Selection Sort — перевірте,
# чи ви, бува, не шукаєте мінімум у межах [0..n-1] на кожному кроці заново.
# Виведіть стан arr після кожного outer loop.

# def selection_sort_standard(arr):
#     pass

# data = [8, 5, 4, 2]
# selection_sort_standard(data)
# print(data)
# ------------------------------------------------------------------


# ЗАВДАННЯ 1
# Напишіть (або використайте) дві функції: selection_sort(arr) та insertion_sort(arr)
# (або bubble_sort(arr)). Згенеруйте декілька різних списків (випадкових, майже відсортованих,
# зворотно відсортованих) і порівняйте кількість обмінів, порівнянь, а також загальний час, який вони витрачають.

# import random
# import time

# def buble_sort(arr):
#     # ваша реалізація

# def selection_sort(arr):
#     # ваша реалізація

# # генерація даних
# arr = [random.randint(1, 1000) for _ in range(1000)]

# порівняння часу

# Порахуйте, скільки обмінів робить Selection і скільки — Insertion / Bubble.
# Подивіться, скільки часу кожний витрачає на різні патерни масиву (вже відсортований, випадковий, зворотно відсортований).
# ------------------------------------------------------------------

# ЗАВДАННЯ 2
# Згенеруйте список, де перші 70% елементів уже відсортовані,
# а решта 30% — випадкові. Перевірте, наскільки Selection Sort
# упорається швидше/повільніше, ніж той самий insertion_sort чи bubble_sort.

# Створіть arr_sorted із 70% відсортованих значень, додайте 30% хаотичних.
# Запустіть selection_sort(arr), заміряйте час.
# Порівняйте з часом для insertion_sort(arr) і bubble_sort(arr) (оптимізованого, якщо треба).
# Зробіть висновок, чи Selection Sort має вигоду з часткового упорядкування.

# def partial_sorted_data(n, ratio=0.7):
#     # створюємо відсортовані дані для int(ratio*n) елементів
#     # а решта - випадкові
#     pass